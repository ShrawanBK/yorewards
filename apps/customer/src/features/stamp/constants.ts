/** Matches `STAMP_PENDING_TTL_MS` in `@repo/supabase/queries/stamps`. */
export const STAMP_PENDING_TTL_MS = 5 * 60 * 1000;

/** Poll while waiting for merchant approve/reject when Realtime is unavailable. */
export const STAMP_STATUS_POLL_MS = 2_000;
